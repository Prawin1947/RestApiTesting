info:
  title: reqRes API
  description: its a test of reqResApi
  version: 0.1.0

  servers:
    url: http://reqres.in/api
    description: its a reqRes api demo URL

    paths:
      /users/2:
        get:
          summary: Returns a list of users.
          description: Returns a list of users for reqRes api.

          responses:
            "200": # status code
              description: A JSON array of user

              content:
                application/json:

                  schema:
                    type: array
                    items:
                      type: string

